// AsyncStorage key shared between SkinAgeScreen (which sets it once the
// scratch-off is revealed) and its entry point on Insights (which reads it
// to decide whether to offer "Reveal" or "View" your skin age).
export const SKIN_AGE_SCRATCHED_KEY = 'sureva_skinage_scratched';
